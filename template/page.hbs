/**
 * 路由：{{ path }}
 */

import React from 'react'
{{#if isLayoutPage}}
import { Outlet } from 'react-router-dom'
{{/if}}

const {{ properCase name }}: React.FunctionComponent = () => {
  return (
    <div>
      {{ properCase name }}
      {{#if isLayoutPage}}
      <Outlet />
      {{/if}}
    </div>
  )
}

export default React.memo({{ properCase name }})
